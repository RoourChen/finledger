# 财会助手 环境变量模板
# 用法：复制为 .env 并填写真实值；.env 已被 .gitignore 忽略，请勿提交。
# 创建后必须 chmod 600 .env，限制仅当前账号可读。
# 启动方式（因 streamlit 不自动读 .env，需手动导出）：
#   set -a; source .env; set +a; .venv/bin/streamlit run app.py
# 注意：哈希写入 .env 即可，切勿直接粘贴到命令行（会进 shell 历史，且同机可见）。
# 安全红线：.env 只由可信运维人员维护；source .env 会按 shell 脚本执行其内容，
#           切勿让不可信内容进入此文件。

# 生产：访问密码哈希（用 .venv/bin/python password_tool.py 生成，务必使用强密码）
# 未设置此项时，若也未设置 FINLEDGER_PASSWORD，应用将拒绝启动。
FINLEDGER_PASSWORD_HASH=

# 本地开发（可选）：明文密码，启动时会明确警告，正式部署请勿使用
# FINLEDGER_PASSWORD=

# 操作人默认值（可选，侧边栏可临时修改）
FINLEDGER_OPERATOR=admin

# 数据库文件路径（可选，默认 ./finledger.db）
# FINLEDGER_DB=/path/to/finledger.db
